# เพิ่มฟีเจอร์กำหนดเวลาในการพิมพ์ (Time Limit)

## 📋 สรุปการเปลี่ยนแปลง

เพิ่มฟีเจอร์กำหนดเวลาในการพิมพ์สำหรับบทเรียนประเภท "พิมพ์ในระบบ" ซึ่งจะนับถอยหลังเวลาและหยุดการพิมพ์อัตโนมัติเมื่อหมดเวลา

## 🎯 ฟีเจอร์ใหม่

### 1. กำหนดเวลาในการพิมพ์ (Time Limit)
- ✅ เพิ่มฟีลด์ `time_limit` (นาที) ในตาราง `typing_assignments`
- ✅ แสดงฟีลด์กำหนดเวลาในหน้าสร้าง/แก้ไขบทเรียน (เฉพาะโหมดพิมพ์ในระบบ)
- ✅ แสดง countdown timer แทน stopwatch เมื่อมีการกำหนดเวลา
- ✅ เปลี่ยนสีตัวเลขเป็นสีแดงเมื่อเหลือเวลาน้อยกว่า 30 วินาที
- ✅ หยุดการพิมพ์และบันทึกผลอัตโนมัติเมื่อหมดเวลา

### 2. การใช้งาน

#### สำหรับผู้สอน (Admin)
1. เข้าหน้า **จัดการบทเรียน** → **สร้างบทเรียนใหม่**
2. เลือกรูปแบบการส่งงาน: **พิมพ์ในระบบ**
3. จะปรากฏฟีลด์ **เวลาในการพิมพ์ (นาที)**
4. กำหนดเวลา เช่น 1, 2, 5 นาที (ค่าเริ่มต้น: 5 นาที)
5. บันทึกบทเรียน

#### สำหรับนักเรียน (Student)
1. เลือกบทเรียนที่ต้องการฝึก
2. เมื่อเริ่มพิมพ์ ระบบจะเริ่มนับถอยหลัง
3. แสดงเวลาที่เหลือ เช่น `05:00`, `04:59`, ...
4. เมื่อเหลือเวลา ≤ 30 วินาที ตัวเลขจะเปลี่ยนเป็นสีแดง
5. เมื่อหมดเวลา (`00:00`) ระบบจะ:
   - หยุดการพิมพ์อัตโนมัติ
   - แสดงผลคะแนน
   - เปิด modal ให้บันทึกผล

## 📁 ไฟล์ที่สร้าง/แก้ไข

### 1. Migration
**ใหม่:** `database/migrations/2026_01_14_080000_add_time_limit_to_typing_assignments_table.php`
```php
Schema::table('typing_assignments', function (Blueprint $table) {
    $table->integer('time_limit')->nullable()->after('max_score')
        ->comment('Time limit in minutes for typing practice');
});
```

### 2. Model
**แก้ไข:** `app/Models/TypingAssignment.php`
- เพิ่ม `'time_limit'` ใน `$fillable` array

### 3. Views

#### Create Assignment
**แก้ไข:** `resources/views/typing/admin/assignments/create.blade.php`
- เพิ่มฟีลด์ "เวลาในการพิมพ์ (นาที)"
- แสดงเฉพาะเมื่อเลือก submission_type = "typing"
- ค่าเริ่มต้น: 5 นาที
- ช่วงค่า: 1-60 นาที

#### Edit Assignment
**แก้ไข:** `resources/views/typing/admin/assignments/edit.blade.php`
- เพิ่มฟีลด์ time_limit (แสดงเฉพาะ submission_type = "typing")

#### Practice (Student)
**แก้ไข:** `resources/views/typing/practice.blade.php`

**JavaScript Changes:**
```javascript
// เพิ่ม variables
timeLimit: @json($assignment->time_limit), // in minutes
timeRemaining: null, // in seconds

// Init: Set countdown timer
if (this.timeLimit && this.timeLimit > 0) {
    this.timeRemaining = this.timeLimit * 60;
}

// startTimer: Countdown และ auto-finish
if (this.timeLimit && this.timeLimit > 0) {
    this.timeRemaining = (this.timeLimit * 60) - this.timer;
    if (this.timeRemaining <= 0) {
        this.timeRemaining = 0;
        this.finishGame(); // Auto-finish
        return;
    }
}
```

**UI Changes:**
```blade
<!-- แสดง countdown timer (ถ้ามี) หรือ stopwatch (ถ้าไม่มี) -->
<p class="text-2xl font-bold" 
   :class="timeRemaining <= 30 && timeRemaining > 0 ? 'text-red-600' : 'text-gray-700'" 
   x-text="formatTime(timeRemaining || timer)">
   00:00
</p>
```

## 🔄 วิธีรัน Migration

เมื่อเปิด database แล้ว ให้รันคำสั่ง:

```bash
php artisan migrate
```

## 💡 ตัวอย่างการใช้งาน

### ตัวอย่าง 1: แบบฝึกหัดพิมพ์ 2 นาที
- **หัวข้อ:** "แบบฝึกหัดพิมพ์สัมผัส บทที่ 1"
- **ประเภทเอกสาร:** บันทึกข้อความ
- **รูปแบบการส่งงาน:** พิมพ์ในระบบ
- **เวลาในการพิมพ์:** 2 นาที
- **คะแนนเต็ม:** 100

นักเรียนจะเห็น countdown: `02:00` → `01:59` → ... → `00:01` → `00:00` (Auto-finish)

### ตัวอย่าง 2: แบบฝึกหัดไม่จำกัดเวลา
- **หัวข้อ:** "พิมพ์เอกสารราชการ"
- **เวลาในการพิมพ์:** ไม่ระบุ (หรือ = 0)

นักเรียนจะเห็น stopwatch ปกติ: `00:00` → `00:01` → `00:02` → ... (ไม่มีการจำกัดเวลา)

## 📊 การคำนวณคะแนน

คะแนนยังคงคำนวณเหมือนเดิม:
```javascript
baseScore = (accuracy * 0.6) + (WPM_Ratio * 0.4)
finalScore = (baseScore / 100) * maxScore
```

**หมายเหตุ:** การจำกัดเวลาอาจส่งผลให้:
- WPM ต่ำลงถ้านักเรียนพิมพ์ไม่ทัน
- Accuracy ลดลงถ้าพิมพ์เร็วเกินไปเพื่อให้ทันเวลา

## 🎨 UI/UX Improvements

1. **Color Coding:**
   - เวลามากกว่า 30 วินาที: สีเทา (gray-700)
   - เวลาน้อยกว่า 30 วินาที: สีแดง (red-600) เพื่อเตือน

2. **Auto-finish:**
   - เมื่อหมดเวลา ระบบจะหยุดการพิมพ์ทันที
   - แสดง modal ผลลัพธ์
   - นักเรียนสามารถบันทึกผลหรือลองใหม่

## 🔧 Technical Details

### Database Schema
```sql
ALTER TABLE typing_assignments 
ADD COLUMN time_limit INT(11) NULL 
COMMENT 'Time limit in minutes for typing practice' 
AFTER max_score;
```

### Frontend Logic Flow
1. **เริ่มพิมพ์:** startTimer() → กำหนด timeRemaining
2. **ทุกๆ 1 วินาที:** 
   - timer++ (นับเวลารวม)
   - timeRemaining = (timeLimit * 60) - timer
   - ตรวจสอบ: ถ้า timeRemaining <= 0 → finishGame()
3. **หมดเวลา:** finishGame() → แสดง modal → บันทึกผล

## 📝 หมายเหตุสำหรับผู้พัฒนา

1. **time_limit = NULL** หรือ **= 0**: ไม่จำกัดเวลา (ใช้ stopwatch)
2. **time_limit > 0**: จำกัดเวลา (ใช้ countdown timer)
3. ฟีลด์แสดงเฉพาะ **submission_type = 'typing'** เท่านั้น
4. ฟีลด์นี้ไม่เกี่ยวกับ due_date (กำหนดส่งงาน)

## ✅ Checklist

- [x] สร้าง migration เพิ่มฟีลด์ time_limit
- [x] อัพเดท Model (TypingAssignment)
- [x] แก้ไข create.blade.php (เพิ่มฟีลด์)
- [x] แก้ไข edit.blade.php (เพิ่มฟีลด์)
- [x] แก้ไข practice.blade.php (countdown timer)
- [x] เพิ่ม UI สีแดงเมื่อเวลาใกล้หมด
- [x] เพิ่ม auto-finish เมื่อหมดเวลา
- [ ] รัน migration (รอ database เปิด)
- [ ] ทดสอบการสร้างบทเรียนใหม่
- [ ] ทดสอบ countdown timer
- [ ] ทดสอบ auto-finish

## 🚀 ขั้นตอนถัดไป

1. รัน `php artisan migrate` เมื่อ database พร้อม
2. ทดสอบสร้างบทเรียนใหม่พร้อมกำหนดเวลา
3. ทดสอบการพิมพ์และ countdown timer
4. อาจเพิ่ม sound alert เมื่อเหลือเวลา 10 วินาที (optional)
5. อาจเพิ่ม progress bar เพื่อแสดงเวลาที่เหลือ (optional)
