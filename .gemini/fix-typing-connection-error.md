# แก้ไขปัญหา: ไม่สามารถเชื่อมต่อกับเซิร์ฟเวอร์ได้ (ระบบแบบฝึกหัดพิมพ์ดีด)

## 🔍 สาเหตุของปัญหา

เมื่อนักเรียนพิมพ์แบบฝึกหัดเสร็จและกดปุ่ม "บันทึกผล" ระบบแสดง error ว่า **"ไม่สามารถเชื่อมต่อกับเซิร์ฟเวอร์ได้"**

**สาเหตุ:**
- ไฟล์ `practice.blade.php` ใช้ **hardcode URL** แบบนี้:
  ```javascript
  fetch(`/typing/student/practice/${this.assignmentId}/submit`, { ... })
  ```
  
- ปัญหาคือ application อาจอยู่ใน **subdirectory** เช่น `/adm/` 
- เมื่อส่ง request ไปที่ `/typing/student/practice/1/submit` แต่ URL จริงควรเป็น `/adm/typing/student/practice/1/submit`
- ทำให้ระบบหา route ไม่เจอ และเกิด error "ไม่สามารถเชื่อมต่อกับเซิร์ฟเวอร์ได้"

## ✅ วิธีแก้ไข

### ไฟล์ที่แก้ไข: `resources/views/typing/practice.blade.php`

**เปลี่ยนจาก:**
```javascript
fetch(`/typing/student/practice/${this.assignmentId}/submit`, { ... })
```

**เป็น:**
```javascript
const submitUrl = "{{ route('typing.student.practice.submit', ':id') }}".replace(':id', this.assignmentId);
fetch(submitUrl, { ... })
```

### การเปลี่ยนแปลงทั้งหมด:

1. **บรรทัด 307-308**: ใช้ `route()` helper เพื่อสร้าง URL ที่ถูกต้อง
   ```javascript
   const submitUrl = "{{ route('typing.student.practice.submit', ':id') }}".replace(':id', this.assignmentId);
   const redirectUrl = "{{ route('typing.student.submissions') }}";
   ```

2. **บรรทัด 333**: เปลี่ยน redirect URL จาก hardcode เป็น variable
   ```javascript
   window.location.href = redirectUrl;
   ```

3. **บรรทัด 79**: แก้ปุ่มกลับหน้าหลัก
   ```blade
   <a href="{{ route('typing.student.assignments') }}" class="btn-outline">
   ```

## 🎯 ประโยชน์ของการแก้ไข

✅ **ทำงานได้ทุก environment** - ไม่ว่าจะอยู่ใน subdirectory หรือ root
✅ **ไม่ต้อง hardcode URL** - Laravel จัดการ URL ให้อัตโนมัติ
✅ **แก้ปัญหา connection error** - ส่ง request ไปที่ URL ที่ถูกต้อง
✅ **ง่ายต่อการ maintain** - ถ้าเปลี่ยน route ก็เปลี่ยนที่เดียวใน `web.php`

## 📝 วิธีทดสอบ

1. เข้าสู่ระบบในฐานะนักเรียน
2. เลือกแบบฝึกหัดที่ต้องการทำ (ประเภท "ฝึกพิมพ์")
3. พิมพ์ข้อความตามที่กำหนดจนครบ
4. กดปุ่ม "บันทึกผล"
5. ควรเห็นข้อความ "บันทึกสำเร็จ!" และถูก redirect ไปหน้า submissions

## 🔧 Route ที่เกี่ยวข้อง

```php
// routes/web.php (บรรทัด 92)
Route::post('/practice/{id}/submit', [TypingController::class, 'store'])
    ->name('typing.student.practice.submit');
```

## 📌 หมายเหตุ

- การแก้ไขนี้ใช้หลักการเดียวกับที่เคยแก้ไขปัญหา PDF path ใน conversation ก่อนหน้า
- เป็นการแก้ปัญหา URL routing ในระบบที่อยู่ใน subdirectory ของ shared hosting
